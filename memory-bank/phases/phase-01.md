# Faz 01: Başlangıç ve Kurulum

## Amaçlar
-   Proje kod tabanı yapısını kurmak.
-   Git deposunu başlatmak.
-   "Merhaba Dünya" diyen bir Backend (Node.js/Express) oluşturmak.
-   "Merhaba Dünya" diyen bir Frontend (React/Vite) oluşturmak.

## Teslim Edilecekler
-   [ ] `/backend` dizini, `package.json`, `tsconfig.json` ve `src/index.ts` ile.
-   [ ] `/frontend` dizini, başlangıç React uygulaması ile.
-   [ ] `/docs/analysis` dizini.
-   [ ] Root dizinde `README.md`.

## Teknik Detaylar
-   **Backend:** Node.js + Express + TypeScript.
-   **Frontend:** React + Vite + TypeScript.
-   **Git:** İlk commit.

## Doğrulama
-   Backend: `npm run dev` hatasız çalışmalı -> localhost:3000 erişilebilir olmalı.
-   Frontend: `npm run dev` hatasız çalışmalı -> localhost:5173 erişilebilir olmalı.
